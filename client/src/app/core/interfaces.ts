export interface User {
    firstName: string
    lastName: string
    email: string
    password: string
    confirmPassword: string
    id?: string
}

export interface Message {
    message: string
  }